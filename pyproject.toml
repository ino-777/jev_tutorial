[project]
name = "jev-tutorial"
version = "0.1.0"
description = "Jev (typesafe-sdk) を使った、TODOアプリのチュートリアル"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "typesafe-sdk>=0.7.1",
    "python-dotenv>=1.0",
]
